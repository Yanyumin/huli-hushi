<view class="tabs">
    <view class="tabs_title">
        <view class="title_item {{item.isActive?'active':''}} " wx:for="{{tabs}}" wx:key="id" bindtap="handleTitleChange" data-index="{{index}}">
                         
           <view class="round">
               
           </view>
             <view class="line">

             </view>
                          <view class="title_name">{{item.title}}</view>
        </view>
      
                     
    </view>
    <view class="tabs_content">
        <slot></slot>
    </view>
</view>